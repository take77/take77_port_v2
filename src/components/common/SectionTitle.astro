---
interface Props {
  title: string;
  subtitle?: string;
  accent?: string;
}

const { title, subtitle, accent } = Astro.props;
---

<div class="mb-12">
  {accent && (
    <p class="text-accent font-heading text-sm font-semibold uppercase tracking-widest mb-2">{accent}</p>
  )}
  <h2 class="font-heading text-heading text-3xl md:text-4xl font-semibold leading-tight">{title}</h2>
  {subtitle && (
    <p class="text-sub mt-3 text-lg">{subtitle}</p>
  )}
</div>

<style>
  .text-accent {
    color: var(--text-accent);
  }

  .text-sub {
    color: var(--text-sub);
  }
</style>
